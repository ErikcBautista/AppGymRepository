@model IEnumerable<AppGym.Models.Bitacoras>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["nameHeader"] = "Bitacora";
    ViewData["head"] = true;
}

<div class="bg-primary text-white text-center" style="padding:5px 0;margin:2px 10px;">
    <a asp-action="Create" class="text-white">Create New</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                Asistencia
            </th>
            <th>
                Empleado
            </th>
            <th colspan="3">
                Acciones
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asistencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoBitacoraNavigation )
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBitacora">Edit</a> |
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.IdBitacora">Details</a> |
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@item.IdBitacora">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
