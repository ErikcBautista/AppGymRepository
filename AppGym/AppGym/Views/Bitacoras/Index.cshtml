@model IEnumerable<AppGym.Models.Bitacoras>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asistencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleadoBitacoraNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asistencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoBitacoraNavigation.ApellidosEmpleado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdBitacora">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdBitacora">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdBitacora">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
